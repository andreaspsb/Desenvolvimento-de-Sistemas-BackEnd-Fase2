generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Pagamento {
  codigo        Int      @id @default(autoincrement())
  codAss        Int
  valorPago     Float
  dataPagamento DateTime
}